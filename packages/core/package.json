{
  "name": "@atelier/core",
  "version": "0.0.8",
  "private": true,
  "description": "Atelier core: deterministic logic, types, and primitives. Internal library; bundled into @gilons-ai/atelier, not published on its own.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean && rm -rf dist",
    "test": "node --test tests/**/*.test.js"
  },
  "license": "MIT",
  "author": "gilons",
  "engines": {
    "node": ">=20.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gilons/atelier.git",
    "directory": "packages/core"
  },
  "dependencies": {
    "yaml": "^2.9.0"
  }
}
